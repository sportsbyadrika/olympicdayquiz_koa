# Deny direct web access to PHP includes.
# These are loaded by PHP via the filesystem, never served over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
